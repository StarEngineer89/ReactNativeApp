import { IPupilState } from './_types';

export const initialState: IPupilState = {
  _id: null,
  image: null,
  name: null,
  classrooms: null,
  loading: true,
  error: null,
  currentCategory: null,
};
