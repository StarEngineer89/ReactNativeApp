import { IPupilState } from './_types';

export const initialState: IPupilState = {
  _id: null,
  image: null,
  name: null,
  classroom: null,
  loading: true,
  error: null,
  currentCategory: null,
};
